# Core (recommended, but resolver/executor degrade gracefully without them)
rapidfuzz       # fuzzy app-name matching; falls back to stdlib difflib if absent

# UI (Cycle 4 "Sakura") — the whole desktop app is Qt Widgets. Essentials
# only (not the full PySide6 metapackage): skips ~100 MB of Addons we never
# import. No QML/QtQuick, no QtWebEngine anywhere in the codebase.
PySide6-Essentials

# Windows-only, optional — each feature that needs one checks for it and fails
# explicitly (never silently) if it's missing
pywin32         # Start Menu .lnk resolution, window management (win32gui), clipboard history
pycaw           # exact volume level + multi-device mute (F3); without it, volume falls back to media keys
comtypes        # required by pycaw
Pillow          # screenshot capture (a_screenshot.py)

# Voice input is a fully optional module: not imported unless the mic/hotkey
# is used, and degrades to an explicit "not set up" message if either package
# (or the downloaded Vosk model) is missing.
# vosk            # offline speech recognition model (~50MB); download separately
# sounddevice     # microphone capture for voice input

# Ed25519 signature verification for update manifests —
# fail-closed: without it, no update ever verifies.
cryptography
